language: java
#addons:
#  apt:
#    packages:
#      - oracle-java8-installer
jdk:
  - oraclejdk8
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
notifications:
  irc:
    channels:
      - "open.ircnet.net#ohtufubar"
    template:
      - "%{repository} (%{commit}) : %{message}  "
      - "Build details: %{build_url}"
